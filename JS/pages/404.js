/**
 * PANADERÍA LUZ MARINA — Página: 404
 * Solo necesita que el header funcione igual que en el resto del sitio
 * (tema claro/oscuro, idioma, menú móvil) — no hay carrito ni catálogo
 * que inicializar acá.
 */

import { initUI } from '../core/ui.js';

document.addEventListener('DOMContentLoaded', () => {
  initUI();
});
